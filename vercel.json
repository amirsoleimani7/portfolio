{
  "installCommand": "npm install --no-audit --no-fund",
  "buildCommand": "npm run build",
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "nodeVersion": "18.x"
      }
    }
  ]
}
